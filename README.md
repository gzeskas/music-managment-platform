### Itunes caching service

Requirements: `Java 11, docker, docker-compose` 

Build command `./gradlew clean build`

Run test `./gradlew clean test`

To run integration tests `./gradlew clean integrationTest`